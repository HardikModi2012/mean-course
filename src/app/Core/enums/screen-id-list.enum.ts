/**
 * List All Screen Names for Screen Table
 *
 * Must Add Same screen id to Audit Type and if Audit functionality is not available then add XXXXX as code to return back
 *
 * @export
 * @enum {number}
 */
export enum ScreenIdList {
  /* Home Screens */
  HOME = 'HOME',
}
